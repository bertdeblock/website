{
  "name": "bertdeblock",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "lint": "concurrently --group --prefix-colors auto --timings \"pnpm:lint:*(!fix)\"",
    "lint:fix": "concurrently --group --prefix-colors auto --timings \"pnpm:lint:*:fix\"",
    "lint:format": "prettier . --cache --check",
    "lint:format:fix": "prettier . --cache --write",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "autoprefixer": "^10.4.19",
    "concurrently": "^8.2.2",
    "nuxt": "^3.11.2",
    "postcss": "^8.4.38",
    "prettier": "^3.3.2",
    "prettier-plugin-tailwindcss": "^0.6.5",
    "tailwindcss": "^3.4.4",
    "vue": "^3.4.27",
    "vue-router": "^4.3.2"
  }
}
